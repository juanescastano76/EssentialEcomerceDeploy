export interface ProductType {
  id: number;
  title: string;
  price: number;
  description: string;
  imgUrl: string;
  quantity: number;
}

export interface FormType {
  email: string;
  firstName: string;
  lastName: string;
  phone: number;
  address: string;
  city: string;
}
